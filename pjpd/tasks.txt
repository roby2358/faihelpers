Priority:   85
Status: ToDo
ID: import-tmpl-9enm
Created: 2026-09-14T00:16:22Z
Updated: 2026-09-14T00:16:22Z

Add an "Import template" button to the Tasks tab (js/tasks_panel.js). Clicking it opens a file picker for a .toml file, parses it with the existing TOML import in js/persist/toml.js, and creates a new task docmem from it (the file's root node becomes the task docmem root). After import, the new task docmem should be selected in the Tasks tab so the harness can be run on it immediately. Reuse the Persist tab's load path rather than duplicating parsing. Update docs/TASK_DELEGATION_SPEC.md to describe the button.
----
Priority:   85
Status: ToDo
ID: add-feedback-p7ev
Created: 2026-09-14T05:58:45Z
Updated: 2026-09-14T05:58:45Z

In js/tasks_panel.js, addTask() returns silently when no task docmem is bound (harness is null) and when the task text is empty, so clicking Add with nothing bound looks like a dead button. startHarness() already shows an error message in the same no-harness case. Make addTask give the same feedback: showMessage('Create or select a task docmem first', 'error') when harness is null, and showMessage('Enter task text first', 'error') when the text is empty. On success, show a short success message naming the new node id, e.g. 'Added task <id>', so the user sees the add land even while the tree is re-rendering during a run. No spec change needed; this is panel feedback only.
----
Priority:   80
Status: ToDo
ID: fence-one-xsmw
Created: 2026-09-14T00:34:21Z
Updated: 2026-09-14T00:34:21Z

Change the pytool format so each ```pytool fence holds exactly one command, and add a heredoc tail: after the closing paren, `<<<` followed by a newline makes everything to the end of the fence the final string argument, with no quoting. Example:
```pytool
docmem_update_content("9yu7wxh6") <<<
Marge slammed a stack of fins on the counter. "Rob didn't show," she said.
```
Rationale: long prose in Python string literals is where parse errors and dropped arguments happen (a paragraph ending in a dialogue quote before """ broke a ten-call batch in a DeepSeek v4 Pro run); the fence itself never fails, and the agent loop already runs every fence in a response, so batching is preserved with one call per fence. Benefits: a parse error discards one call instead of the whole batch, and each fence maps to exactly one result line.
Work: edit js/pytool/pytool.pegjs (remove the statement list, add the heredoc tail rule, make a second command in a fence fail with the message "one command per fence"), regenerate js/pytool/pytool_parser.js with peggy, update js/pytool/SPEC_PYTOOL_PARSER.md, update js/pytool/test_pytool_parser.html with cases for heredoc, quote-terminated content, and the two-commands error, reword js/docmem_tools/docmem_prompt.js and js/system_tools prompts so "share a block" becomes "in the same response" and show one heredoc example near the top, and append an entry to docs/ARCHITECTURE_LOG.md.
----
Priority:   75
Status: ToDo
ID: persist-bzb3
Created: 2026-07-03T01:55:47Z
Updated: 2026-07-03T01:55:47Z

Implement persistence for the SQLite database using IndexedDB so that docmem data survives page reloads and browser sessions. Serialize the database state to IndexedDB on save operations and restore it when loading a docmem instance.
----
Priority:   70
Status: ToDo
ID: status-ctx-5egg
Created: 2026-09-14T06:07:05Z
Updated: 2026-09-14T06:07:05Z

Move a task node's status out of the {key=value} state block in its text and into the node's context_value, the way summary nodes already carry done or failed in context_value. Task nodes become context_type "task", context_name = lens, context_value = one of queued, running, waiting, done, failed. The state block keeps only the run ledger: attempts, failures, chat, read, model, retry_limit, updated.

Code (js/task_harness.js): selectNext/isEligible read node.contextValue, not the parsed block; an empty or unknown context_value on a task node is an error (fail fast, no fallback to queued). Remove the queued fallback from parseStateBlock, which returns an empty state when no block is present. resetStaleRunning, runTask, applyTermination and terminate write status via docmem.updateContext(nodeId, node.contextType, node.contextName, status) and the ledger via the existing writeState; termination makes two calls, no combined Docmem update is added. The task message tells workers to create child tasks with docmem_create_node("append-child", <id>, "task", "", "queued", <instruction>) and drops the "text starting with {status=queued}" wording.

Panel (js/tasks_panel.js): addTask appends with context_value "queued" and plain instruction text; setStatus uses updateContext; renderRow shows the status from context_value beside the ledger block.

Spec: update docs/TASK_DELEGATION_SPEC.md (state block, task node shape, selection, termination, worker child-task instructions) to the new layout and append a docs/ARCHITECTURE_LOG.md entry. Existing dumps in toml/ keep their old blocks; they are read-only fixtures and need no migration. Verify in js/test_task_harness.html: selection ignores a stale status key in the block, an empty context_value throws, and finish/fail set both context_value and the summary.
----
Priority:   65
Status: ToDo
ID: sensory-3eiu
Created: 2026-07-02T06:44:10Z
Updated: 2026-07-03T01:55:48Z

Add a "sensory" docmem to the chat context: ambient situational information such as current time, location, temperature/weather, and other random sensory details. It goes at the very end of the message list — after the recency-sorted docmem expansions — which fits the most-volatile-last ordering, since it changes every turn anyway (per SPEC_CHAT's cache-stability ordering; keeping it last means its churn invalidates nothing else).
----
Priority:   65
Status: ToDo
ID: global-ids-9xnx
Created: 2026-07-03T01:55:48Z
Updated: 2026-07-03T01:55:48Z

Remove the docmem state from DocmemCommands and instead treat node-ids as global (and globally unique)
----
Priority:   60
Status: ToDo
ID: stop-budget-uyg6
Created: 2026-09-07T00:33:57Z
Updated: 2026-09-07T00:33:57Z

Add a Stop button wired to an AbortController that cancels the running AgentLoop and any delegated children, retry with backoff for 5xx and network errors in OpenRouterAPI, and a per-run token budget that terminates the loop with a budget_exhausted reason. Required before an unattended task queue can run safely.
----
Priority:   60
Status: ToDo
ID: model-record-fmjt
Created: 2026-09-14T00:27:49Z
Updated: 2026-09-14T00:27:49Z

TOML dumps do not record which model ran a chat, so dump reviews cannot attribute behavior to a model. Record the OpenRouter model id on each chat session docmem when the chat is created or when the first request is sent (e.g. in the chat root's context value alongside the date, or as a state key on the task harness chat root). Cover both chat-panel chats and task-harness chats (js/chat.js, js/task_harness.js). Update docs/SPEC_CHAT.md to describe the field.
----
Priority:   55
Status: ToDo
ID: docmem-tests-6h9i
Created: 2026-07-03T02:05:54Z
Updated: 2026-07-03T02:05:54Z

Create a browser test page for docmem core invariants, like the existing parser test pages (js/bash/test_command_parser.html). Cover: hash formula includes exactly parent_id|context_type|context_name|context_value|text|order (not readonly); order interpolation stays monotonic under repeated insertion; optimistic locking raises OptimisticLockError on concurrent modification; expandToLength never orphans a node (every included node's parent is included) and totalCount is accurate; focus registry set/clear/fallback-on-deleted-node behavior. These invariants currently have no automated coverage and regress silently.
----
Priority:   55
Status: ToDo
ID: lenses-xk5d
Created: 2026-09-07T00:33:59Z
Updated: 2026-09-07T00:33:59Z

Add editing-lens seed docmems for fiction passes (continuity, pacing, dialogue, prose rhythm, POV discipline, and a drafting lens), each a readonly system-prompt docmem in js/system_prompts. Add a fiction seed with arcs, characters, timeline, and scenes per docs/WHITEPAPER_NARRATIVE_COHERENCE_ENGINE.md. A task names its lens and the runner includes only that lens plus the root prompt.
----
Priority:   50
Status: ToDo
ID: compress-dv9d
Created: 2026-09-07T05:03:17Z
Updated: 2026-09-07T05:03:17Z

Size-triggered chat compression. The agent loop records turns flat under the chat root and never summarizes on its own; compression happens only when the model calls docmem_add_summary on a range of its own messages or the user does it from the UI. Add a trigger: when the chat docmem's token count (or the built message list) exceeds a configurable threshold, prompt the model, via the per-turn $ System.turn() message, to summarize the oldest contiguous range of messages with docmem_add_summary, and surface the same nudge in the Chat tab for the user. Do not summarize automatically without a model or user action. Update SPEC_CHAT with the threshold and the nudge text, and note the decision in ARCHITECTURE_LOG.
----
Priority:   50
Status: ToDo
ID: chat-summ-8nka
Created: 2026-09-07T05:20:32Z
Updated: 2026-09-07T05:21:30Z

Let the model summarize its own chat history. Today the chat docmem is hidden from the model: chat_ roots are filtered out of the roster and expansions, so it never sees its chat root ID or its message node IDs, and the prompts never say the conversation is a docmem. Asked to summarize the conversation, the model instead copied facts into a new docmem and summarized that. Fix: (1) tell the model in the docmem prompt that its conversation is a chat docmem whose messages are sibling leaf nodes under the chat root, and that it may compress older history by calling docmem_add_summary on a contiguous range of them; (2) give it the IDs it needs, preferably through the per-turn $ System.turn() message, e.g. the chat root ID and the IDs of the oldest and newest messages not yet under a summary, without expanding the whole chat into context; (3) verify the summary node is then sent as the collapsed tool-call pair and its children are omitted. Keep chat roots out of the roster otherwise. Alternatives to weigh: the harness could do it automatically (the loop picks the range and makes a separate model call to write the summary text, then calls addSummary itself), or a new dedicated tool such as chat_summarize(summary_text) that takes only the text and lets the framework choose the range, so the model never needs message IDs. Update SPEC_CHAT and add an ARCHITECTURE_LOG entry.
----
Priority:   50
Status: ToDo
ID: reasoning-v6tx
Created: 2026-09-12T23:36:27Z
Updated: 2026-09-12T23:36:27Z

Make reasoning a checkbox on the Chat page. Currently `REASONING` is a constant in js/agent_loop.js (true), sent as OpenRouter's `reasoning.enabled` in OpenRouterAPI.buildRequestBody and shown in the chat status line. Add an "Include reasoning" checkbox next to the model dropdown in index.html, read it in chat.js / tasks_panel.js when constructing the AgentLoop, and pass it through explicitly (no default parameter) so both the Chat tab and the task harness honor it. Fable requires reasoning on; small models run cheaper with it off.
----
Priority:   50
Status: ToDo
ID: narrow-guard-cf7v
Created: 2026-09-14T05:57:32Z
Updated: 2026-09-14T05:57:32Z

In js/tasks_panel.js, guardStopped() refuses every hand edit (edit, requeue, move, delete) on every task node while the harness is started. The hazard it exists to prevent is narrower: hand edits to the node the harness is currently writing (harness.current), and deleting that node or one of its ancestors, which makes the harness's terminal writeState throw and stops the whole loop. Under the node-ownership write model in docs/TASK_DELEGATION_SPEC.md (Who writes when), editing a queued sibling, requeuing a failed task, or reordering what runs next are safe during a run and are the sideways steering the continuous-loop goal wants. Replace the state-keyed guard with a node-keyed one: refuse edit/requeue/move on the running node, refuse delete on the running node and its ancestors, allow everything else while started. Keep the docmem-switch refusal keyed on harness state. Update the Start and Stop section of TASK_DELEGATION_SPEC (currently "every other hand edit MUST be refused while started") to the new rule, and append an ARCHITECTURE_LOG entry. Verify with the harness running that moving a queued sibling changes which task runs next without an OptimisticLockError.
----
Priority:   45
Status: ToDo
ID: ctx-schema-85zu

Define a context schema scheme that specifies the structure and validation rules for context metadata (context_type, context_name, context_value) used across docmem nodes
----
Priority:   45
Status: ToDo
ID: req-log-hixf
Created: 2026-07-03T02:05:57Z
Updated: 2026-07-03T02:05:57Z

Persist raw LLM request/response pairs per turn for post-hoc debugging. OpenRouterAPI.logRequest currently dumps to console only, which evaporates on reload — when a small model does something baffling, the interesting request is often gone. Keep an in-memory ring buffer of (request messages JSON, response, timestamp, model) and add a download/export from the Persist tab. Also enables diffing turn N vs N+1 message lists to verify prompt-cache prefix stability is actually holding.
----
Priority:   45
Status: ToDo
ID: node-updated-fv7r
Created: 2026-09-12T07:01:11Z
Updated: 2026-09-12T07:01:11Z

Add an updated_at timestamp column to the docmem node definition (DuckDB schema, Node class in js/docmem_tools/docmem_types.js, TOML round-trip in js/persist/toml.js). Set it on every create and update. Keep it out of the hash formula. Once present, the task harness should use it instead of the `updated` key it currently writes in task state blocks, and TASK_DELEGATION_SPEC.md should drop that key.
----
Priority:   45
Status: ToDo
ID: cmd-registry-9tp8
Created: 2026-09-14T08:16:38Z
Updated: 2026-09-14T08:16:38Z

Collapse the per-command facts scattered across js/command_router.js and js/docmem_tools/docmem_commands.js into one command registry. Today adding a docmem command means editing four places: the KNOWN_DOCMEM_COMMANDS set, the STATIC_DOCMEM_COMMANDS set (commands that run without an active docmem), the NODE_ID_ARGS table (which argument positions hold node ids, used to resolve the reserved "last" reference), and the switch in executeDocmemCommand that checks arity and calls the handler. The "places a node" fact is implied only by whether a handler's result carries placedId. Replace these with a single table keyed by command name whose entry holds: the usage string and minimum arity, whether it needs an active docmem, the node-id argument positions, whether it places a node (sets "last"), and the handler function. Derive KNOWN_DOCMEM_COMMANDS, the static set, and the last-resolution positions from the table, and drive dispatch from it so the switch goes away. Keep result wording and error messages unchanged so js/test_command_router.html and the prompt examples still hold; extend that test page with a check that every entry in KNOWN_DOCMEM_COMMANDS has a registry entry. Append a docs/ARCHITECTURE_LOG.md entry; no spec change since behavior is unchanged.
----
Priority:   40
Status: ToDo
ID: view-meta-4a6m
Created: 2026-07-03T01:55:49Z
Updated: 2026-07-03T01:55:49Z

Add a checkbox to the View page that allows users to toggle showing or hiding metadata. When checked, display node metadata (context_type, context_name, context_value, token counts, order values, timestamps, etc.) alongside the content. When unchecked, show only the content without metadata. This provides a cleaner view option for reading documents while still allowing access to structural information when needed.
----
Priority:   40
Status: ToDo
ID: lock-ctx-9phx

Add a docmem-lock-context function that accepts a context schema and applies validation/constraints to ensure nodes conform to the specified context schema structure
----
Priority:   40
Status: ToDo
ID: node-path-v5y6

Support @root-id/node-id syntax to reference and relate docmem nodes across different docmem instances. This enables cross-document node references using a path-like notation that combines the root docmem ID with the specific node ID.
----
Priority:   40
Status: ToDo
ID: ro-cache-wj6d

Support caching read-only docmems by ordering them first in the system messages and marking them with cache_control ephemeral. Read-only docmems should be placed before dynamic docmems in the message list, with cache control applied, so they can be cached separately from frequently-changing docmem contexts.
----
Priority:   40
Status: ToDo
ID: sys-status-48jb
Created: 2026-07-03T02:08:57Z
Updated: 2026-09-07T01:51:30Z

Extend the framework-generated $ System.turn() user message (the final message of every turn, see SPEC_CHAT) with a small status readout: turn count, agent depth (root vs delegated), token budget usage, and pending delegation state. Follows the fake-call conventions: System. prefix, plain undecorated output. It sits at the very end of the list so its per-turn churn invalidates no cacheable prefix.
----
Priority:   40
Status: ToDo
ID: add-selected-2fxp
Created: 2026-09-14T05:12:10Z
Updated: 2026-09-14T05:12:10Z

Add an "Add to selected" button to the Tasks tab (js/tasks_panel.js) next to "Add task". "Add task" always appends the new task node as the last child of the task root. "Add to selected" appends it as the last child of the selected task node instead, so the user can queue a subtask by hand. Disable or refuse it when no task node is selected, when the selected node is a summary, or when the selected node is the task currently running (the worker may be creating children under it). Appending under a queued or waiting task is safe in any harness state, since a node's hash does not cover its children. Update docs/TASK_DELEGATION_SPEC.md, Start and Stop section, to describe both controls.
----
Priority:   35
Status: ToDo
ID: arch-ann-i2di
Created: 2026-07-03T01:55:50Z
Updated: 2026-09-07T00:34:07Z

Architect semantic search for docmem. Embeddings come from OpenRouter; storage is DuckDB WASM (not sql.js), so evaluate: (1) a FLOAT[] embedding column with array_cosine_similarity and brute-force scan, adequate for a single novel's worth of nodes; (2) the DuckDB vss extension if loadable in WASM; (3) a JS-side HNSW library (hnswlib-wasm) alongside DuckDB. Document the chosen approach. Defer until explicit task read-sets prove insufficient.
----
Priority:   35
Status: ToDo
ID: wiki-view-atq9
Created: 2026-07-03T01:55:49Z
Updated: 2026-07-03T01:55:49Z

Display a docmem as a wiki page with proper formatting and navigation. Render docmem content as a readable wiki page with formatted text, headers, sections, and navigation elements. This includes styling the content appropriately and providing a user-friendly interface for browsing docmem content as wiki documentation.
----
Priority:   35
Status: ToDo
ID: wiki-toc-5cps
Created: 2026-07-03T01:55:49Z
Updated: 2026-07-03T01:55:49Z

Add a table of contents feature for docmem wiki pages. Generate and display a hierarchical table of contents based on the structure of wiki page nodes, allowing users to navigate through sections and subsections of the wiki content. The TOC should be auto-generated from the docmem node hierarchy.
----
Priority:   35
Status: ToDo
ID: replay-qux7
Created: 2026-07-03T02:06:00Z
Updated: 2026-07-03T02:06:00Z

Replay harness: take a logged request messages array, edit it (e.g., tweak one system message), resend via OpenRouterAPI.chat(), and compare responses side by side. OpenRouterAPI.chat() already accepts a messages array, so this is roughly a textarea plus send button on a dev tab. Gold for small-model prompt iteration. Depends on req-log for capturing the requests to replay.
----
Priority:   35
Status: ToDo
ID: auto-struct-ynxd
Created: 2026-07-03T02:06:03Z
Updated: 2026-07-03T02:06:03Z

For docmems whose context serialization is truncated ([partial: ...]), consider auto-including the omitted structure as a second framework-generated fake system call, e.g. $ System.docmem_structure_to_context("<id>") followed by the skeleton of the subtrees that were left out. Saves the agent a turn of calling docmem_structure itself. Follow the established fake-call conventions: System. prefix, name distinct from the real docmem_structure command if output differs, byte-stable output for prompt caching.
----
Priority:   35
Status: ToDo
ID: chat-retain-fjbv
Created: 2026-09-12T05:51:10Z
Updated: 2026-09-12T05:51:10Z

Define and implement a retention policy for chat docmems. Every agent run creates a chat_ docmem, and under the task harness each task node owns one (recorded in its state block), so they accumulate for the life of the page. Everything is in-memory today, so this is not urgent, but decide: keep a task's chat until the task is folded and then delete it, keep the N most recent, keep until the user clears them from the Tasks panel, or fold old chats into a summary node. Whatever the policy, the Tasks panel should let the user open the chat for any task node while it still exists, and TOML save/load should treat retained chats consistently.
----
Priority:   30
Status: ToDo
ID: fts-3633
Created: 2026-07-03T01:55:48Z
Updated: 2026-09-07T00:34:04Z

Full-text search for docmem beyond literal/wildcard/regex: ranked (BM25-style) search with phrase and boolean queries. The store is DuckDB WASM, not sql.js, so FTS5 does not apply; evaluate the DuckDB fts extension availability in WASM, or a JS-side inverted index (e.g. minisearch) rebuilt from the nodes table. Lower priority now that literal search exists and tasks carry explicit read-sets.
----
Priority:   30
Status: ToDo
ID: db-mgmt-fayc

Implement database management functions for docmem instances. Add operations to clear/delete docmem documents, list all available docmem instances, view metadata about stored documents, and perform maintenance tasks. This will provide users with tools to manage their collection of docmem documents and clean up unused or outdated instances.
----
Priority:   30
Status: ToDo
ID: download-3ck4

Add a download button that exports a serialized docmem document. Implement functionality to serialize the current docmem tree structure into a downloadable format (JSON, text, or other format) so users can save and share docmem documents. The download should include the full hierarchical structure with all nodes, their content, context information, and relationships.
----
Priority:   30
Status: ToDo
ID: chat-cache-eq84
Created: 2026-07-02T07:22:14Z
Updated: 2026-07-02T07:22:14Z

Set cache_control: { type: 'ephemeral' } on the last chat message when building the message list, so providers with explicit (Anthropic-style) prompt caching cache the conversation history prefix. Currently only the two prompt system messages carry breakpoints, so the append-only chat history is re-billed as uncached input every turn on those providers. The marker moves forward each turn (standard incremental pattern); docmem expansions stay after it. ~3 lines in docmem_chat.js buildMessageList; harmless on providers that ignore the field. Low priority: this harness targets smaller models with implicit caching, where the existing message ordering already pays off. Related: ro-cache-wj6d (breakpoints for read-only docmems).
----
Priority:   30
Status: ToDo
ID: empty-nodes-bh7g
Created: 2026-07-03T02:06:05Z
Updated: 2026-07-03T02:06:05Z

formatNodesExpanded in docmem_chat.js renders empty-text nodes (e.g., bare root nodes) as a metadata line followed by a blank line before the first --- separator. Consider filtering empty-text nodes from the expanded serialization the way serialize() does, or rendering them metadata-only, to tidy the docmem context messages. Cosmetic; noticed during message-list review of the docmem_focus work.
----
Priority:   25
Status: ToDo
ID: focus-scope-xdr7
Created: 2026-07-03T02:06:08Z
Updated: 2026-07-03T02:06:08Z

Docmem focus state (Docmem.focusByRoot) is global per docmem root, so a parent agent and its delegated child share focus — if the child focuses a subtree, the parent's next turn also sees only that subtree. Currently accepted as intentional (docmems are shared by design), but revisit whether focus should be scoped per chat/agent (e.g., key the registry by chatId+rootId) once delegation gets real use. Design decision, not a bug.
----
Priority:   10
Status: ToDo
ID: ctxvisible-4xwb
Created: 2026-09-12T19:07:54Z
Updated: 2026-09-12T19:30:06Z

ON HOLD (2026-09-12): models get tool-happy, and a command that lets them read more would let them blow up their own context. The docmem_create hook that adds a created docmem to the live read-set and the task's `read` key is expected to cover the observed problem. Revisit only if a run shows a worker starved of context it cannot get by creating a docmem or by a user-set `read=` key.

Original proposal: give agents explicit control over which docmem nodes are expanded into their context: commands to add a node (or docmem root) to the context and to remove one, usable by both the chat agent and task workers. Today only task workers have a read-set, built from the task's `read` state key and extended automatically when the worker creates a docmem; the chat agent sees every non-chat docmem and docmem_focus narrows one docmem to a subtree. Design so the new commands and the read-set are one mechanism, not two: e.g. the add/remove commands edit the task's `read` key when run by a worker, and edit an equivalent per-chat list when run by the chat agent, with docmem_focus either folded in or clearly subordinate. The commands must take effect on the live read-set for the next model call, not only at the next run: the read-set is currently computed once at run start, so a worker that writes `read=` into its own state block sees nothing until it suspends and reruns. The worker task message must name the commands (or the `read` key); today nothing the worker sees mentions the read-set, which is why a worker that had every chapter id from search and docmem_structure spent 78 turns reconstructing the story from snippets. Update TASK_DELEGATION_SPEC (Worker context, Task message), SPEC_CHAT (docmem context messages), and the docmem_focus result text, which currently promises a full-tree serialization that a scoped read-set does not honor.